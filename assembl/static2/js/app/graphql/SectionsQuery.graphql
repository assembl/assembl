query SectionsQuery($lang: String) {
  hasResourcesCenter
  hasSyntheses
  sections {
    id
    sectionType
    order
    title(lang: $lang)
    titleEntries {
      value
      localeCode
    }
    url
  }
}
