#import "./fragments/Translation.graphql"

query UserPrefernces($id: ID!) {
  user: node(id: $id) {
    ... on AgentProfile {
      id
      preferences {
        harvestingTranslation {
          ...translation
        }
      }
    }
  }
}
