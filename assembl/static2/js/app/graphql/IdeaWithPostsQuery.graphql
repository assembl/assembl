query IdeaWithPosts($id: ID!) {
  idea: node(id: $id) {
    ... on Idea {
      id
      posts {
        edges {
          node {
            ... on Post {
              id
              parentId
              creationDate
            }
          }
        }
      }
    }
  }
}
