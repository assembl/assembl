query IdeaWithPosts($id: ID!, $lang: String) {
  idea: node(id: $id) {
    ... on Idea {
      id
      numPosts
      numContributors
      messageColumns {
        color
        header(lang: $lang)
        index
        messageClassifier
        name(lang: $lang)
        numPosts
        title(lang: $lang)
      }
      posts {
        edges {
          node {
            ... on Post {
              id
              parentId
              creationDate
              publicationState
              originalLocale
              messageClassifier
              sentimentCounts {
                like
                disagree
                dontUnderstand
                moreInfo
              }
            }
          }
        }
      }
    }
  }
}
