fragment IdeaMessageColumn on IdeaMessageColumn {
  color
  columnSynthesis {
    id
    subject(lang: $lang)
    body(lang: $lang)
  }
  index
  messageClassifier
  name(lang: $lang)
  numPosts
  title(lang: $lang)
}
