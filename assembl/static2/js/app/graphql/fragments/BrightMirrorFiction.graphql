fragment BrightMirrorFiction on Post {
  subject(lang: $contentLocale)
  body(lang: $contentLocale)
  creationDate
  publicationState
  creator {
    userId
    displayName
    isDeleted
    image {
      externalUrl
    }
  }
}
