mutation createPost($ideaId: ID!, $subject: String, $body: String!) {
  createPost(ideaId:$ideaId, subject: $subject, body: $body) {
    post {
      ... on PropositionPost {
        id,
        subject,
        body,
        creator {
          id,
          name
        }
      }
    }
  }
}