#import "./fragments/Ideas.graphql"

mutation updateIdeas($discussionPhaseId: Int!, $ideas: [IdeaInput]!) {
  updateIdeas(discussionPhaseId: $discussionPhaseId, ideas: $ideas) {
    query {
      ...ideas
    }
  }
}
