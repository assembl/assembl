<div class="idealist-body idealist-body--synthesis">

    <div class="idealist-counter"><span data-toggle="tooltip" title="{{ gettext('Number of syntheses in the discussion') }}"><%= num_synthesis_posts %></span></div>
    <div class="idealist-title" title="{{ gettext('View syntheses') }}">
        {{ gettext('View syntheses') }}
    </div>
</div>
