<div class="panel-body">

    <div class="mbs">
         <h4 class="pls">{{ gettext('Annotation on the web') }}</h4>
         <p class="pls">{{ gettext('If you desire catching extracts out of assembl (exemple : Wikipedia),') }}
         <a href="#" class="js_bookmark">{{ gettext('click here to activate the annotation') }}</a> </p>
    </div>

    <h4 class="pls">{{ gettext('Extracts not yet associated with an idea') }}</h4>
    <div class="postitlist">
    </div>
    {#
    <div class="box box--dashed box--centered margin text-muted">
        {{ gettext("Add segments") }}
    </div>
    #}
    <div class="box box--topborder">
        <a id="segmentList-clear" class="button"> <i class="icon-trash"></i> {{ gettext('Clear') }} &nbsp;</a>
    </div>

</div>

