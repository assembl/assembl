<div class="define-columns">
  <form>
    <div>
      <label>{{ gettext('Activate multi-column view') }}</label>
      <input type="checkbox" class="js_activate_columns"
      <% if (active) { print('checked="checked"'); } %>
      >
    </div>
    <div class="js_messageColumnsList"></div>
    <div>
      <button class="js_add_column btn">{{ gettext('Add') }}</button><br/>
    </div>
  </form>
</div>
