<% if (count > 0) { %>
    <span><%= linked_to_msg %></span>
    <span>
        <% _.each(first, function(n){ %>
            <a class="idea-name js_idea-classification-idea"><%= n %></a>
        <% })%>
        <% if (rest.length > 0 ) { %>
            <%= other_ideas_msg %>
        <% } %>
    </span>
<% } %>
