<div class="popin-header">
	<h3 class="h3"><i class="icon-reload"></i> {{ gettext('assembl need to be reload') }}</h3>
</div>

<div class="popin-body">
  <div class="content-ajaxError">
  	<p class="mbm">{{ gettext('Assembl a problem that requires recharging. Do not worry, your writing current messages will not be lost.') }}</p>
  	<p class="mbm">{{ gettext('see you in a minute on Assembl!') }}</p>
  	<p class="mbxs msg-error">{{ gettext('If you are in contact with the technical team, please tell them the following message:') }}</p>
  	<p class="msg-error"><%= msg %></p>
  </div>
  <div class="clr"></div>
</div>

<div class="popin-footer">
	<a href="#" class="error-close fl js_close">{{ gettext('dismiss') }}</a>
  <button class="btn btn-third btn-sm fr js_reload">{{ gettext('reload') }}</button>
</div>
