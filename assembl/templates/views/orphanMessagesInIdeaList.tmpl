<div class="idealist-body">
    <span class="idealist-space"></span>

    <div data-toggle="tooltip" class="idealist-counter" title="{{ gettext('Messages not yet associated with an idea') }}">
        <span><%= num_orphan_posts %></span>
    </div>
    {# TODO <% var num_unread_posts = (num_posts - num_read_posts); %>#}
    <div title="{{ gettext('Messages top-posted and whose thread has not yet been harvested into an idea') }}" class="idealist-title idealist-title-unread">
        {{ gettext('Messages yet to be organised') }}
        {#<span data-toggle="tooltip" class="idealist-title-unread-count js_idealist-title-unread-count mlxs" title="{{ gettext('Number of unread syntheses') }}"> <%= num_unread_posts %> </span>#}
    </div>
</div>