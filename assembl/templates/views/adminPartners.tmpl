<div class="gr">
  <div class="gu gu-1of5">
    <nav class="sidebar-nav">
        <ul class="man pan adminMenu">
          <li>
            <a href="#edition" class="menu">{{ gettext('Edit discussion') }}</a>
          </li>
          <li>
            <a href="#settings" class="menu">{{ gettext('Server settings') }}</a>
          </li>
          <li>
            <a href="#notifications" class="menu">{{ gettext('Edit notifications') }}</a>
          </li>
          <li class="active">
            <a href="#partners" class="menu">{{ gettext('Edit partners') }}</a>
          </li>
          <li class="mtxl">
            <a href="{{ discussion.get_url() }}">{{ gettext('Back to discussion') }}</a>
          </li>
        </ul>
    </nav>
  </div>
  <div class="gu gu-3of4 admin-section">
    <section class="section">
    <div class="admin-title">
      <h3 class="h3">{{ gettext('Partners') }}</h3>
    </div>
    <div id="content-container" class="content-container">

    <div class="bx bx-alert-success hidden">
			<button class="btn btn-cancel btn-sm bx-close" type="button">
				<i class="icon-delete"></i>
			</button>
			{{ gettext("Partner successfully added") }}
		</div>

		<div class="mal tar">
			<a href="#" class="js_addPartner"><i class="icon-add"></i>&nbsp; {{ gettext("Add partner") }}</a>
		</div>

	  <div id="partner-content" class="phl"></div>

    </div>
    </section>
  </div>
</div>


