<% if( !hideHeader ) { %>
    <header class="panel-header">
        <span class="panel-header-title"
        <% if( tooltip ) { %>
            data-toggle="tooltip" data-placement="top" title="<%= tooltip %>"
        <% } %> ><%= title %></span>
        <div class="fr">
            <% if( userCanChangeUi ){ %>
                <% if( hasLock ){ %>
                    <a class="js_lockPanel lock-group phs fr"><i class="iconbutton icon-lock-open" data-toggle="tooltip" data-placement="top"  title="{{ gettext('Lock panel') }}"></i></a>
                <% } %>
                <a class="js_panel-fullScreenButton iconbutton iconbutton--resizefull fr " data-toggle="tooltip" data-placement="top"  title="{{ gettext('Fullsize') }}"></a>
                <% if ( hasClose ){ %>
                    <a class="js_panel-closeButton iconbutton icon-delete panel-header-close fr " data-toggle="tooltip" data-placement="top" title="{{gettext('Close panel')}}"></a>
                <% } %>
            <% } %>

            <% if ( hasMinimize ){ %>
                <a class="js_minimizePanel iconbutton panel-header-minimize" data-toggle="tooltip" data-placement="top" title="{{gettext('Minimize panel')}}"></a>
            <% } %>
        </div>
    </header>
<% } %>

<div class="panelContents"></div>

{# TODO: template system for minified state of each panel #}
<div class="panelContentsWhenMinimized <% if ( hideHeader ) { %>hasNoHeader<% } else { %>hasHeader<% } %>">
<% if ( hideHeader ) { %>
    <header class="panel-header">
        <a class="js_minimizePanel iconbutton panel-header-minimize" data-toggle="tooltip" data-placement="top" title="{{gettext('Maximize panel')}}"></a>
    </header>
<% } %>
<span class="icon-idea"></span><span class="icon-home"></span>
</div>