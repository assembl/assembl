<div class="idealist-body idealist-body--rootidea">

    <div class="idealist-counter"><span data-tooltip="{{ gettext('Number of posts in the entire discussion') }}"><%= num_posts %></span></div>
    <div class="idealist-title" title="{{ gettext('View all posts in this discussion') }}">
        {{ gettext('View all posts in this discussion') }}
    </div>
</div>

<div class="idealist-children"></div>
