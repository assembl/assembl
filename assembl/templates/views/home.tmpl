<div class="phm pvxs"><!-- we use a padding here to compensate the margin created by the following h3, which is not included in the computation of the height of the accordion element => so the accordion bottom position is preserved -->
	<h3>{{ gettext('How it works') }}</h3>

	<div>
	{{ gettext('<p>How to participate in the debate "%(debate_name)s":</p>
	<ol>
		<li>Explore the debate using the ideas that have already emerged (do you want to name this as the table of ideas?)</li>
		<li>Join in the conversation by replying directly to the contributions of participants</li>
		<li>Start a new topic of discussion if you wish to rejuvenate the debate around a particular idea</li>
	</ol>')|format(debate_name=discussion.topic)|safe }}
	</div>

	<h3>{{ gettext('Join the debate') }}</h3>

	<p>{{ gettext('Because your opinion merits to be heard.') }}</p>

	<div class="mal bac tac">
		<input type="submit" value="{{ gettext('Join the debate') }}" class="btn btn-primary btn-sm go-to-debate" />
	</div>
</div>