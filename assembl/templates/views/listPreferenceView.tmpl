<div>
<div class="js_listPreference"></div>
<% if (canModify) { %>
<button class="js_add_to_listpref" <% if (!canModify) { print('disabled="disabled"')} %>>{{ gettext('Add') }}</button>
<% } %>
</div>
