<div class="gr">
  <div class="gu gu-1of5">
    <nav class="sidebar-nav">
        <ul class="man pan adminMenu">
          <li>
            <a href="#users/edit" class="menu">{{ gettext('profile') }}</a>
          </li>
          <li class="active">
            <a href="#users/notifications" class="menu">{{ gettext('notifications') }}</a>
          </li>
          <li class="mtxl">
            <a href="/{{ discussion.slug }}/">{{ gettext('back to discussion') }}</a>
          </li>
        </ul>
    </nav>
  </div>
  <div class="gu gu-3of4 admin-section">

    <div class="admin-title">
      <h3 class="h3">Notifications</h3>
    </div>
    <div id="content-container" class="content-container">
      <div class="gr">
        <div class="gu gu-1of4">
          <h4>{{ gettext('Currently you will be informed when :') }}</h4>
        </div>
        <div class="gu gu-1of2">
          <form class="core-form">
            <fieldset>
              <% _.each(UserNotifications, function(subscription, index, list){ %>

                <div class="control-group">
                  <label for="<%= subscription.get('@id') %>" class="control-label"></label>
                  <div class="controls">
                    <label class="checkbox">
                      <input type="checkbox" class="js_userNotification" value="<%= subscription.get('@type') %>" <%= subscription.get('status') === 'ACTIVE' ? 'checked':'' %> id="<%= subscription.get('@id') %>">
                      <%= subscription.get('@type') %>
                    </label>
                  </div>
                </div>

              <% }) %>
            </fieldset>
          </form>
          <form class="core-form">
            <fieldset>
            YES, the rest of the list is incorrect, waiting for proper API point
              <% _.each(addableGlobalSubscriptions, function(subscription, index, list){ %>

                <div class="control-group">
                  <label for="<%= subscription.get('@id') %>" class="control-label"></label>
                  <div class="controls">
                    <label class="checkbox">
                      <input type="checkbox" class="js_userNewNotification" value="<%= subscription.get('@type') %>" <%= subscription.get('status') === 'ACTIVE' ? 'checked':'' %> id="<%= subscription.get('@id') %>">
                      <%= subscription.get('@type') %>
                    </label>
                  </div>
                </div>

              <% }) %>
            </fieldset>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>