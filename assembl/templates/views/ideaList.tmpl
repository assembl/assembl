
    <%
    var titles = {
        'toc': "{{ gettext('Table of ideas') }}",
        'featured': "{{ gettext('Featured ({0})') }}",
        'synthesis': "{{ gettext('In next Synthesis ({0})') }}"
    };

    titles.toc = titles.toc.replace('{0}', tocTotal);
    titles.featured = titles.featured.replace('{0}', featuredTotal);
    titles.synthesis = titles.synthesis.replace('{0}', synthesisTotal);
    %>

    <!--<header>
        {# <div class="dropdown">
            <span class="dropdown-label text-bold"><%= filter === 'featured' ? titles.featured : filter === 'inNextSynthesis' ? titles.synthesis : titles.toc %></span>
            <ul class="dropdown-list">
                <!--<li id="ideaList-filterByFeatured" class="dropdown-listitem"><%= titles.featured %></li>-->
                <li id="ideaList-filterByToc"class="dropdown-listitem"><%= titles.toc %></li>
                <li id="ideaList-filterByInNextSynthesis" class="dropdown-listitem"><%= titles.synthesis %></li>
            </ul>
        </div> #}
        <span class="text-bold"><%= titles.toc %></span>

        <div class="fr">
            <a id="ideaList-graphButton" class="iconbutton icon-flow-tree" data-toggle="tooltip" title="{{gettext('Graph of ideas')}}"></a>
            <a id="ideaList-collapseButton" data-toggle="tooltip" title="<%= collapsed ? '{{gettext('Expand all')}}' : '{{gettext('Collapse all')}}' %>" class="iconbutton <%= collapsed ? 'icon-download-1' : 'icon-upload' %>"></a>
            <a id="ideaList-fullscreenButton" class="iconbutton iconbutton--resizefull" data-toggle="tooltip" title="{{gettext('Fullsize')}}"></a>
            <a id="ideaList-closeButton" class="icon-cancel panel-header-close" data-toggle="tooltip" title="{{gettext('Close panel')}}"></a>
        </div>
    </header>-->

    <% if (canAdd) { %>
      <div class="panel-body panel-body--has-footer">
    <% } else { %>
      <div class="panel-body">
    <% } %>
        <div class="pvxs">
          <span class="pls txt-gray1"><%= titles.toc %> <span class="title-counter"><%= tocTotal %></span> </span>
          <!--<% if (canAdd) { %>
          <a href="#" class="prs fr" id="ideaList-addbutton"><i class="icon-add"></i> {{ gettext('Add idea') }}</a>
          <% } %>-->
        </div>
        <div id="idealist-list" class="idealist"></div>
        <div id="idealist-graph" style="display: none;">
            <div id="infovis" width="100%" height="100%"></div>
            <div id="inner-details"></div>
        </div>
    </div>

    <div class="panel-bodyabove"></div>
    <div class="panel-bodybelow"></div>

    <% if (canAdd) { %>
    <!--<footer class="panel-footer tar">
        <button class="btn btn-default" id="ideaList-addbutton"><span class="icon-add"></span>{{ gettext('Add idea') }}</button>
    </footer>-->
    <% } %>

